<Window x:Class="Prakt18_praktika_.DeleteFilter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prakt18_praktika_"
        mc:Ignorable="d"
        Title="DeleteFilter" Height="276" Width="432" ResizeMode="CanMinimize">
    <Grid>
        <Menu VerticalAlignment="Top">
            <MenuItem Header="Выйти" Click="Quit_Click"></MenuItem>
        </Menu>
        <GroupBox x:Name="Radius1" Header="Удалить товар с ценой" Margin="33,18,0,116">
            <Grid>
                <TextBox x:Name="Zena" HorizontalAlignment="Left" Margin="21,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" Width="120"/>
                <Button Content="Удалить" HorizontalAlignment="Left" Margin="309,0,0,0" VerticalAlignment="Center" Click="DeleteFilter_Click"/>
                <Label Content="цена" HorizontalAlignment="Left" Margin="21,12,0,0" VerticalAlignment="Top"/>
            </Grid>
        </GroupBox>
        <GroupBox x:Name="Radius2" Header="Удалить товар с количеством" Margin="33,144,0,5">
            <Grid>
                <TextBox x:Name="Kol" HorizontalAlignment="Left" Margin="19,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" Width="120"/>
                <Button Content="Удалить" HorizontalAlignment="Left" Margin="310,0,0,0" VerticalAlignment="Center"  Click="DeleteFilter_Click"/>
                <Label Content="количество" HorizontalAlignment="Left" Margin="19,4,0,0" VerticalAlignment="Top"/>
            </Grid>
        </GroupBox>
        <RadioButton x:Name="R1" HorizontalAlignment="Left" Margin="10,81,0,0" VerticalAlignment="Top" Checked="R1_Checked" IsChecked="True"/>
        <RadioButton x:Name="R2" HorizontalAlignment="Left" Margin="10,200,0,0" VerticalAlignment="Top" Checked="R2_Checked"/>
    </Grid>
</Window>
